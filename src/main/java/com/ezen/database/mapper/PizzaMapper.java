package com.ezen.database.mapper;

import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.annotations.Select;

import com.ezen.database.dto.Pizza;

public class PizzaMapper {
	
	//@Select("insert into")//"select employee_id, first_name, last_name, job_id, salary, commission_pct, department_id from employees")
	//public Pizza insertPizza(@Param("id") long id);

}
